# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: devops-project
metadata:
  template: devops-project@0.0.1
services:
  api:
    project: ./src/api
    language: js
    host: appservice
infra:
  provider: terraform